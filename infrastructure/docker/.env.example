# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=vanguard
POSTGRES_USER=vanguard
POSTGRES_PASSWORD=vanguard_dev_password

# Application Configuration
LOG_LEVEL=INFO
