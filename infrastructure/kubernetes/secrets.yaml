apiVersion: v1
kind: Secret
metadata:
  name: vanguard-secrets
  namespace: vanguard
type: Opaque
stringData:
  POSTGRES_PASSWORD: "vanguard_prod_password_CHANGE_ME"
  # In production, use external secrets management (AWS Secrets Manager, Vault, etc.)
